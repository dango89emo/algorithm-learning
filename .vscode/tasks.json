{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build b_tree",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "-std=c++17",
                "${workspaceFolder}/tree/btree.cpp",
                "-o",
                "${workspaceFolder}/tree/btree"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}